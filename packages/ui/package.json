{
  "name": "@dodorail/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Shared UI primitives (shadcn components + brand theme) for DodoRail apps.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'no lint for ui yet'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
