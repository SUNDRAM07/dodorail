/**
 * @dodorail/ui — Shared UI primitives.
 *
 * For Day 1 this is a placeholder. shadcn components live in apps/web/src/components/ui
 * (per shadcn's app-local pattern). We'll promote anything genuinely shared here
 * once we have three+ surfaces using the same component.
 */

export {};
