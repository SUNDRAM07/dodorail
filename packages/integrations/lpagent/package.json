{
  "name": "@dodorail/lpagent",
  "version": "0.1.0",
  "private": true,
  "description": "LP Agent (Nimbus Data Labs) API integration — Meteora DLMM pool discovery, zap-in/out, position metrics, and Copy-LP for merchant idle-treasury yield.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
