{
  "name": "@dodorail/lpagent",
  "version": "0.0.1",
  "private": true,
  "description": "LP Agent API integration — zap-in/positions/zap-out for idle-treasury yield. Placeholder for Day 11-12.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
