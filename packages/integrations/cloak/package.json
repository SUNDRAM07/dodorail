{
  "name": "@dodorail/cloak",
  "version": "0.0.1",
  "private": true,
  "description": "Cloak integration — UTXO shielded pool with browser-native Groth16 ZK proofs for private merchant payments on Solana mainnet.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@cloak.dev/sdk": "^0.1.5",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
