{
  "name": "@dodorail/goldrush",
  "version": "0.0.1",
  "private": true,
  "description": "GoldRush (Covalent) integration — REST BalanceService + Streaming WebSocket. Placeholder for Day 7-10.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
