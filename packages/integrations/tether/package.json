{
  "name": "@dodorail/tether",
  "version": "0.0.1",
  "private": true,
  "description": "Tether asset family on Solana — native USDT, USDT0 (LayerZero OFT cross-chain), XAUT0 (omnichain Tether Gold).",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
