{
  "name": "@dodorail/magicblock",
  "version": "0.0.1",
  "private": true,
  "description": "MagicBlock Private Payments API — TDX attestation. Conditional on API key approval by Day 10.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
