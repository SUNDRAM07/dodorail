{
  "name": "@dodorail/ika",
  "version": "0.0.1",
  "private": true,
  "description": "Ika dWallet integration — bridgeless BTC/ETH via 2PC-MPC threshold signatures. Architectural-only against the Solana pre-alpha (mock signer).",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
