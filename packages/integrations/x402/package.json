{
  "name": "@dodorail/x402",
  "version": "0.1.0",
  "private": true,
  "description": "x402-on-Solana integration — HTTP 402 Payment Required protocol for autonomous agents. Inbound (merchant API gates a resource behind a USDC payment) + outbound (agent pays Zerion-style data calls keylessly).",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
