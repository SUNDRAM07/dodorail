{
  "name": "@dodorail/x402",
  "version": "0.0.1",
  "private": true,
  "description": "x402-on-Solana integration — inbound (agent pays merchant API) + outbound (agent pays Zerion calls). Placeholder for Day 13-14.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
