{
  "name": "@dodorail/sns",
  "version": "0.0.1",
  "private": true,
  "description": "Solana Name Service integration — resolve + reverseLookup + per-merchant subdomains. Placeholder for Day 7-8.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
