{
  "name": "@dodorail/sdk",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types, Solana helpers, and brand tokens used across DodoRail web, agent, and docs.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./brand": "./src/brand.ts",
    "./rails": "./src/rails.ts"
  },
  "scripts": {
    "lint": "echo 'no lint for sdk yet'",
    "type-check": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
