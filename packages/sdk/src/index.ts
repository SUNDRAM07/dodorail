/**
 * @dodorail/sdk — Shared primitives.
 *
 * Consumed by apps/web and dodorail-agent (separate repo). Keep tree-shakable:
 * no side effects, no top-level imports that pull in heavy deps.
 */

export * from "./brand";
export * from "./rails";
