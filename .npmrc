# Works across filesystems (Windows mount <-> Linux sandbox, or WSL).
# Use copy instead of hardlink so installs don't fail on cross-fs boundaries.
package-import-method=copy
# Enable strict peer deps review but don't block on warnings.
strict-peer-dependencies=false
auto-install-peers=true
# Faster installs during iteration.
prefer-workspace-packages=true
